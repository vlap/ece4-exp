- base.context:
    experiment:
      monitoring:
        activate: true
    model_config:
      components: ['xios', 'nemo'] # Include 'ismm' to activate ism coupling
      nemo:
        grid: "eORCA1_ISO"
